# Development env example
# Enable proxy mode to use local server (/api) instead of running models in-browser
VITE_USE_HF_PROXY=true

# Hugging Face API token (required if using proxy to call HF inference API)
# Obtain from https://huggingface.co/settings/tokens
HF_API_TOKEN=your_hf_api_token_here
